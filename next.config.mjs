/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                protocol: "https",
                hostname: "production.na01.natura.com",
                port: '', // Deja vacío si no estás usando un puerto específico
                pathname: '/on/demandware.static/**', // Puedes ajustar el patrón según la URL de las imágenes
            },
            {
                protocol: "https",
                hostname: "production.na01.natura.com",
                port: '', // Deja vacío si no estás usando un puerto específico
                pathname: '/on/demandware.static/**', // Puedes ajustar el patrón según la URL de las imágenes
            },
            {
                protocol: "https",
                hostname: "production.na01.natura.com",
                port: '', // Deja vacío si no estás usando un puerto específico
                pathname: '/on/demandware.static/**', // Puedes ajustar el patrón según la URL de las imágenes
            },
            {
                protocol: "https",
                hostname: "production.na01.natura.com",
                port: '', // Deja vacío si no estás usando un puerto específico
                pathname: '/on/demandware.static/**', // Puedes ajustar el patrón según la URL de las imágenes
            },
            {
                protocol: "https",
                hostname: "production.na01.natura.com",
                port: '', // Deja vacío si no estás usando un puerto específico
                pathname: '/on/demandware.static/**', // Puedes ajustar el patrón según la URL de las imágenes
            },
            {
                protocol: "https",
                hostname: "production.na01.natura.com",
                port: '', // Deja vacío si no estás usando un puerto específico
                pathname: '/on/demandware.static/**', // Puedes ajustar el patrón según la URL de las imágenes
            },
            {
                protocol: "https",
                hostname: "production.na01.natura.com",
                port: '', // Deja vacío si no estás usando un puerto específico
                pathname: '/on/demandware.static/**', // Puedes ajustar el patrón según la URL de las imágenes
            },
        ],
    }
};

export default nextConfig;
